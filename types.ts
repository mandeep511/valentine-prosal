export interface Position {
  x: number;
  y: number;
}

export interface FloatingElement {
  id: number;
  x: number;
  y: number;
  scale: number;
  rotation: number;
  delay: number;
  duration: number;
  emoji: string;
}
